{
  "name": "@claro/search-nav",
  "version": "1.0.0",
  "scripts": {
    "build": "rimraf dist && tsc && rollup -c rollup.config.js",
    "start:build": "yarn build && es-dev-server --root-dir dist --app-index index.html --compatibility none --watch --open"
  },
  "devDependencies": {
    "@babel/core": "^7.10.2",
    "@open-wc/building-rollup": "^1.3.1",
    "deepmerge": "^4.2.2",
    "es-dev-server": "^1.54.0",
    "rimraf": "^3.0.2",
    "rollup": "^2.13.0",
    "typescript": "^3.9.3"
  },
  "dependencies": {
    "@polymer/paper-input": "^3.2.1",
    "lit-element": "^2.3.1"
  }
}
